class SnowArray
    def initialize(elements)
        @elements = elements
    end
    def read(index)
        @elements[index]
    end
    def write(member, value)
        @elements[index] = value
    end
end